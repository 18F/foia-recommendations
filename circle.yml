machine:
  node:
    # https://discuss.circleci.com/t/use-node-version-from-package-json/122/7
    version: 6.10.3

dependencies:
  override:
    - make setup
  post:
    - make build

test:
  override:
    - make test
